# PlantUML JSON/YAML Transformers

This companion extension registers JSON and YAML transformers into `local.vscode-plantuml-core-trans`.

Registered keys:

- `json2puml` for `.json`
- `yaml2puml` for `.yaml` and `.yml`

It depends on the core extension and contributes no commands of its own.
